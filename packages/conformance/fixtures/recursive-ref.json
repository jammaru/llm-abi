{
  "type": "object",
  "properties": {
    "name": { "type": "string" },
    "friend": { "$ref": "#/$defs/Person" }
  },
  "required": ["name"],
  "$defs": {
    "Person": {
      "type": "object",
      "properties": {
        "name": { "type": "string" },
        "friend": { "$ref": "#/$defs/Person" }
      },
      "required": ["name"]
    }
  }
}
