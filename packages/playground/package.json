{
  "name": "@llm-abi/playground",
  "private": true,
  "description": "Browser playground that compiles one schema against every built-in llm-abi target.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "llm-abi": "workspace:*"
  },
  "devDependencies": {
    "vite": "^8.2.1"
  }
}
