---
"llm-abi": patch
---

Rewrite the published package README so npm shows the four compatibility labels, the docs site, and that the GPT-5.6 Chat Completions tools example is unsupported. Pin the GitHub Action example to `@v0.5.0` instead of `@v0.2.0` or `@latest`.
