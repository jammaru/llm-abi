---
"llm-abi": minor
---

Add a Runtime ABI next to compile() and checkRequest(): checkDeployment(), runtime-scoped schema targets for llama.cpp / LM Studio / Ollama, and a Node-only llm-abi/local doctor and smoke probe. Default check() stays provider-only. The qwen alias remains Alibaba Model Studio.
